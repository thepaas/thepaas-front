import { Header, Page } from '@components';
import { BottomTabs } from '@features';

const Home = () => {
  return (
    <Page>
      <Header />
      <BottomTabs />
    </Page>
  );
};

export default Home;
