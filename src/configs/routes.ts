const PREFIX = '/front';

export default {
  home: { path: `${PREFIX}/` },
  bank: { path: `${PREFIX}/government` },
  add: { path: `${PREFIX}/add` },
  ticket: { path: `${PREFIX}/events` },
  government: { path: `${PREFIX}/milestones` },
};
